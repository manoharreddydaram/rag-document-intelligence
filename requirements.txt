# CPU-only torch wheel index (avoids pulling multi-GB CUDA binaries this
# project doesn't need). Works both locally and on Streamlit Community Cloud.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0

fastapi==0.139.0
uvicorn[standard]==0.51.0
streamlit==1.58.0
python-multipart==0.0.32

# Parsing
pymupdf==1.25.5
python-docx==1.1.2

# Chunking
langchain-text-splitters==0.3.9

# Embeddings / classification / reranking
sentence-transformers==3.1.1
transformers==4.46.3

# Vector store / sparse retrieval
chromadb==1.0.15
rank-bm25==0.2.2

# LLM generation - Groq (OpenAI-compatible endpoint, via the `openai` package)
openai==1.109.1

# Evaluation (pinned together deliberately: ragas 0.2.15 hard-imports a
# langchain_community module removed in langchain-community>=0.4, so the
# whole langchain family below is pinned to the last compatible 0.3.x line).
# langchain-openai also serves as the RAGAS judge LLM wrapper (pointed at
# Groq's OpenAI-compatible endpoint).
ragas==0.2.15
langchain==0.3.27
langchain-core==0.3.86
langchain-community==0.3.27
langchain-openai==0.2.14
datasets==5.0.0

# Utilities
python-dotenv==1.2.1
pydantic==2.11.5
numpy==2.5.1
pytest==9.0.1
